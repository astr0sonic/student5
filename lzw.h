#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;

std::vector<int> compress(const std::string& text);
std::string decompress(const std::vector<int>& compressed);